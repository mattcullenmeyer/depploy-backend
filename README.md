## Run Locally

`$ go run server.go`  
http://localhost:8080/

## Run Docker

`$ docker compose up -d`  
`$ docker compose down`

## Initialize

`$ go mod init github.com/mattcullenmeyer/depploy-backend`
